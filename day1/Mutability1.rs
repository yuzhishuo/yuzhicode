fn main(){

    // To mutate a value, 
    // we must first use the mut keyword to make a variable binding mutable instead.
    let mut a_number = 10;
    println!("the number is {}.", a_number);

    a_number = 15;

    println!("and now the number is {}." a_number);
}